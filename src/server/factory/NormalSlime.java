package server.factory;

import java.awt.Color;
import server.Slime;

public class NormalSlime extends Slime {
    public NormalSlime(double x, double y, Color color, boolean isLeft) {
        super(x, y, color, isLeft);
    }
}
