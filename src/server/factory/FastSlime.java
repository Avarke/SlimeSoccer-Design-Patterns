package server.factory;

import java.awt.Color;
import server.Slime;

public class FastSlime extends Slime {
    public FastSlime(double x, double y, Color color, boolean isLeft) {
        super(x, y, color, isLeft);
    }
}
