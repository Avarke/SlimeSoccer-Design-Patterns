package server.factory;

import java.awt.Color;
import server.Slime;

public class LightSlime extends Slime {
    public LightSlime(double x, double y, Color color, boolean isLeft) {
        super(x, y, color, isLeft);
    }
}
