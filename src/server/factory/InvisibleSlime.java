package server.factory;

import java.awt.Color;
import server.Slime;

public class InvisibleSlime extends Slime {
    public InvisibleSlime(double x, double y, Color color, boolean isLeft) {
        super(x, y, color, isLeft);
    }
}
